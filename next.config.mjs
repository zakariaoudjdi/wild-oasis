/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [{ protocol: "https", hostname: "ouoazikuuwahqwozkyrs.supabase.co" }],
 
  },

};

export default nextConfig;
